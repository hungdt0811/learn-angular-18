export type ProductItem = {
    id: number,
    name: string,
    price: number,
    image: string
}

export type ItemBlog = {
    id?: number,
    title?: string,
    body?: string,
    author?: string
}